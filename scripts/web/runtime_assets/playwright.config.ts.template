
import {{ defineConfig }} from '@playwright/test';

export default defineConfig({{
  testDir: '{test_dir}',
  timeout: 20000,
  retries: 0,
  fullyParallel: true,
  workers: 4,
  use: {{
    baseURL: '{base_url}',
    headless: true,
    viewport: {{ width: 1920, height: 1080 }},
  }},
  reporter: [['json', {{ outputFile: '{results_file}' }}]],
}});
